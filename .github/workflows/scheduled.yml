name: Scheduled Trigger
on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  create_issue:
    runs-on: ubuntu-latest
    steps:
      - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create -t "$(date +'%Y-%m-%d')" -b "Amin Chouaibi"